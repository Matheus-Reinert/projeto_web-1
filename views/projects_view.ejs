<div class="card" style="width: 18rem;">
  <img src="<%= `/images/${project.image}`%>" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">
      <%= project.name %>
    </h5>
    <p><%= project.description %></p>
    <a href="/projects/<%= project.id %>" class="btn btn-primary">Ler mais</a>
  </div>
</div>